# Scripts

This directory is reserved for future command-line entrypoints.

Phase 0 intentionally provides no evaluator scripts. Future scripts must remain thin wrappers around package code and must follow `docs/DEE Evaluation Protocol Final.md`.
